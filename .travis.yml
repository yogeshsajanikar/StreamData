language: c

env:
 global:
  - EnableNuGetPackageRestore=true 

install:
  - sudo apt-get install mono-devel mono-gmcs nunit-console

script:
  - nuget restore FileBlob.sln
  - xbuild /property:Configuration=Release FileBlob.sln
